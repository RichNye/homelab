- name: set up mealplanner frontend on web servers
  hosts: web_servers
  remote_user: "{{ remote_user }}"
  become: yes

  tasks:
    - name: Install Docker
      include_role:
        name: geerlingguy.docker
      vars:
        docker_users:
          - "{{ remote_user }}"

    - name: 
        


